<h1>Users controller, new action</h1>

<p>Edit this file in <tt>app/views/users/new.html.erb</tt></p>

<%= error_messages_for :user %>

<% form_for(@user, :action => url(:users) ) do |f| %>
  <p>
    <%= submit_button "Create" %>
  </p>
<% end %>

<%= link_to 'Back', url(:users) %>
